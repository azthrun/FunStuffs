require 'rho/rhoapplication'

class AppApplication < Rho::RhoApplication
  def initialize
    @tabs = nil
    @@toolbar = nil
    @default_menu = {
      "Refresh" => :refresh
    }
    super
  end
end
